import { HotkeyManager } from '@app/api';
import { Manager } from '@app/core';
import { log } from '@shared/utils';


import * as config from '@app/appconfig';


async function main(){

    
   /* 
   const hotkeyManager = new HotkeyManager({ type:"a"}); //new HotkeyManager();
    const conf = config.HOTKEYMANAGER_CONFIG["hotkeys"];
    
    
    conf.map(h => hotkeyManager.addHotkey(h));
    log("hotkeyConfig", conf, hotkeyManager, hotkeyManager.keyMap);
    const result = await hotkeyManager.run();
    log("result", result);
    */

}
main();
 