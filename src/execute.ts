import { spawn, execFile } from 'child_process';
import { default as execa } from 'execa';
import { } from '@Modules/string/src/path';
 
 

function matchAll(input: string){
    //const [orig, k, cmd, value] = input.match(/(.*)::(\w*)\((.*)\)/ig);
}